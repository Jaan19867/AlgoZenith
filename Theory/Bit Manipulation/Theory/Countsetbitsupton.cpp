#include<bits/stdc++.h>

using namespace std;
// need to count set bits upto n number 
// count the number of set bits of all numbers upto the number  
// $n$  (inclusive), we can run the Brian Kernighan's algorithm on all numbers upto  
// $n$ . But this will result in a "Time Limit Exceeded" in contest submission

void solve(){
    
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

  
        solve();
}