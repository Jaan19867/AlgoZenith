#include<bits/stdc++.h>

using namespace std;

// Knapsack Problem 
// You are given n items , each with a weight and a value 
// we can take any fraction of the item 
// you are given a knapsack of capactiy w
// What is the maximum value you can achieve by putting items in the knapsack ? 
// weight=[2,3,4,5]
// value=[3,4,5,6]

// w=5



void solve(){

    // greedy= sort by value/weight 
    // tell why greedy wont' work if fractions are not allowed ? 
    
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin>>T;
    while(T--)
        solve();
}