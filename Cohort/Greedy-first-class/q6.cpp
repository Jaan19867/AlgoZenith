#include<bits/stdc++.h>

using namespace std;


  // two arrays are given of n elements each . 
  // you can rearrange the elments of the two arrays
  // sum of a[i]+ b[i] should be minimum 

  // a=[1,2,3,4,5]
  // b=[4,5,1,3,1]
  // dot product =1*4 + 2*5 + 3*1+4*3 + 5*1=
void solve(){
    
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin>>T;
    while(T--)
        solve();
}