#include<bits/stdc++.h>

using namespace std;

void solve(){
    int n;
    cin>>n;

    // now we have to design a permutaion from 1 to n 
    // such that their abs diffierence between agal bagal will be from
    // 2 to 4 
    
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin>>T;
    while(T--)
        solve();
}