#include<bits/stdc++.h>

using namespace std;

void solve(){
    int n ; 
    cin>>n;

    // answer wahi hoga jo ki 2^k maximum ho n se just chhota ho 

 // solve this question by using without __bitwidth
      
      for( int bit=0;bit<31;bit++){

        if((1<<bit))
      }


    cout<<(1>>x)-1<<'\n';
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin>>T;
    while(T--)
        solve();
}