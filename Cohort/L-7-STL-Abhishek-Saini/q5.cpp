#include<bits/stdc++.h>

using namespace std;

void solve(){
    // Problem 
    // You are given a list of n integers 
    // Your task is to find the number of pairs ( i,j) such that i<j and 
    // A[i] !=A[j]

    // 
}

int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin>>T;
    while(T--)
        solve();
}